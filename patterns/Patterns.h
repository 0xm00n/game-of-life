
namespace Patterns {
    void create_blinker(Simulator& sim, int x, int y);
    void create_block(Simulator& sim, int x, int y);
    void create_glider(Simulator& sim, int x, int y);
    void create_toad(Simulator& sim, int x, int y);
}
